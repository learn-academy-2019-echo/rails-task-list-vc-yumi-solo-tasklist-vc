<%= link_to "< Back", task_lists_path %>
<h1>Task List: <%= @task_list.title %></h1>

<h2>The description</h2>
<p><%= @task_list.description %> </p>


<h2>The tasks - basic links</h2>
<ul>
  <% @task_list.tasks.each do |task| %>
    <li><%= link_to task.title, "/tasks/#{task.id}" %></li>
  <% end %>
</ul>

<h2>The tasks using route helpers</h2>
<h3>(hint, you need to add an "as: " to the routes for this)</h3>
<ul>
  <% @task_list.tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
